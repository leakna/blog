import { Outlet } from "react-router-dom";
import MakeUpInfo from "../makeupinfo";
const SoftMakeup = () => {
  return (
    <div>
      <MakeUpInfo title="Soft Makeup" urlname="softmakeup" />
    </div>
  );
};

export default SoftMakeup;
